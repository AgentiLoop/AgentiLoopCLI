Extra Sublime-syntax grammars bundled into the binary (syntect's default set
lacks these). Loaded at first use in `src/highlight.rs`.

- Swift.sublime-syntax — from quiqueg/Swift-Sublime-Package (MIT, LICENSE-Swift),
  converted to .sublime-syntax by the bat project.
- TOML.sublime-syntax — from jasonwilliams/sublime_toml_highlighting (MIT, LICENSE-TOML).
