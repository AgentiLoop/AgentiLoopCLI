● Wire provider + default model (5 tasks)
[1] edit_file: Replaced 1 occurrence in /Users/toddbruss/Documents/GitHub/AgentiLoopCLI/crates/agentiloop-cli/src/main.rs [verified: true]

📎mod permission;
📎
📎use std::io::{self, BufRead, Write};
📎use std::sync::Arc;
📎
📎use agentiloop_core::{Agent, AgentConfig, AgentEvent, ToolContext};
📎use agentiloop_provider::
❌AnthropicProvider
✅{AnthropicProvider, ModelInfo}
📎;
📎use anyhow::Result;
📎use clap::Parser;
📎
📎/// AgentiLoop — a cross-platform agentic coding loop for your terminal.
📎#[derive(Parser, Debug)]
📎#[command(name = "agentiloop", version, about)]
📎struct Cli {
📎    /// Model id to use.
📎    #[arg(short, long, env = "AGENTILOOP_MODEL", default_value = "claude-sonnet-4-5")]
📎    model: String,
📎
📎    /// Skip all permission prompts (dangerous; intended for CI).
📎    #[arg(long, env = "AGENTILOOP_YES")]
📎    yes: bool,
📎
📎    /// Max provider round-trips per prompt.
📎    #[arg(long, default_value_t = 50)]
📎    max_turns: usize,
📎
📎    /// Working directory the agent operates in (defaults to cwd).
📎    #[arg(short = 'C', long)]
📎    cwd: Option<std::path::PathBuf>,
📎
📎    /// One-shot prompt. If omitted, starts an interactive REPL.
📎    prompt: Vec<String>,
📎}
📎
📎#[tokio::main]
📎async fn main() -> Result<()> {
📎    tracing_subscriber::fmt()
📎        .with_env_filter(tracing_subscriber::EnvFilter::from_default_env())
📎        .with_writer(io::stderr)
📎        .init();
📎
📎    let cli = Cli::parse();
📎
📎    let cwd = match cli.cwd {
📎        Some(p) => p.canonicalize()?,
📎        None => std::env::current_dir()?,
📎    };
📎
📎    let provider = Arc::new(AnthropicProvider::from_env()?);
📎    let tools = agentiloop_tools::default_registry();
📎    let policy = permission::policy(cli.yes);
📎    let config = AgentConfig { model: cli.model, max_turns: cli.max_turns, ..Default::default() };
📎
📎    let mut agent = Agent::new(provider, tools, policy, config, ToolContext { cwd: cwd.clone() });
📎
📎    if !cli.prompt.is_empty() {
📎        return agent.run(&cli.prompt.join(" "), render).await;
📎    }
📎
📎    eprintln!("AgentiLoop — cwd: {}  model: {}  (/help for commands)", cwd.display(), agent.model());
📎    let stdin = io::stdin();
📎    loop {
📎        eprint!("\n> ");
📎        io::stderr().flush()?;
📎        let mut line = String::new();
📎        if stdin.lock().read_line(&mut line)? == 0 {
📎            break;
📎        }
📎        let line = line.trim();
📎        if line.is_empty() {
📎            continue;
📎        }
📎        if line == "/exit" || line == "/quit" {
📎            break;
📎        }
📎        if line.starts_with('/') {
📎            slash_command(line, &mut agent)?;
📎            continue;
📎        }
📎        if let Err(e) = agent.run(line, render).await {
📎            eprintln!("error: {e:#}");
📎        }
📎    }
📎    Ok(())
📎}
📎
📎/// Fallback catalog used when `/v1/models` can't be reached (newest first).
📎const FALLBACK_MODELS: &[(&str, &str)] = &[
📎    ("claude-fable-5-1", "Claude Fable 5.1"),
📎    ("claude-opus-5", "Claude Opus 5"),
📎    ("claude-sonnet-5", "Claude Sonnet 5"),
📎    ("claude-fable-5", "Claude Fable 5"),
📎    ("claude-opus-4-8", "Claude Opus 4.8"),
📎    ("claude-opus-4-7", "Claude Opus 4.7"),
📎    ("claude-sonnet-4-6", "Claude Sonnet 4.6"),
📎    ("claude-opus-4-6", "Claude Opus 4.6"),
📎    ("claude-opus-4-5-20251101", "Claude Opus 4.5"),
📎    ("claude-haiku-4-5-20251001", "Claude Haiku 4.5"),
📎    ("claude-sonnet-4-5-20250929", "Claude Sonnet 4.5"),
📎];
📎
📎/// Live model list from the API, falling back to the static catalog.
📎async fn fetch_models(provider: &AnthropicProvider) -> Vec<ModelInfo> {
📎    match provider.list_models().await {
📎        Ok(list) if !list.is_empty() => list,
📎        Ok(_) => fallback_models(),
📎        Err(e) => {
📎            tracing::warn!("model list fetch failed, using fallback: {e:#}");
📎            fallback_models()
📎        }
📎    }
📎}
📎
📎fn fallback_models() -> Vec<ModelInfo> {
📎    FALLBACK_MODELS
📎        .iter()
📎        .map(|(id, name)| ModelInfo { id: id.to_string(), display_name: name.to_string(), created_at: String::new() })
📎        .collect()
📎}
📎
📎async fn slash_command(line: &str, agent: &mut Agent, provider: &AnthropicProvider) -> Result<()> {
📎    let (cmd, arg) = line.split_once(' ').map_or((line, ""), |(c, a)| (c, a.trim()));
📎    match cmd {
📎        "/clear" => {
📎            agent.clear();
📎            eprintln!("context and tool history cleared");
📎        }
📎        "/model" if !arg.is_empty() => {
📎            agent.set_model(arg);
📎            eprintln!("model: {}", agent.model());
📎        }
📎        "/model" => {
📎            let models = fetch_models(provider).await;
📎            for (i, m) in models.iter().enumerate() {
📎                let mark = if m.id == agent.model() { "*" } else { " " };
📎                let date = m.created_at.get(..10).unwrap_or("");
📎                eprintln!("{mark} {:>2}. {:<22} {:<28} {date}", i + 1, m.display_name, m.id);
📎            }
📎            eprint!("select [1-{}] or type a model id (enter to keep {}): ", models.len(), agent.model());
📎            io::stderr().flush()?;
📎            let mut pick = String::new();
📎            io::stdin().lock().read_line(&mut pick)?;
📎            let pick = pick.trim();
📎            if pick.is_empty() {
📎                return Ok(());
📎            }
📎            match pick.parse::<usize>() {
📎                Ok(n) if (1..=models.len()).contains(&n) => agent.set_model(models[n - 1].id.clone()),
📎                Ok(_) => {
📎                    eprintln!("out of range");
📎                    return Ok(());
📎                }
📎                Err(_) => agent.set_model(pick),
📎            }
📎            eprintln!("model: {}", agent.model());
📎        }
📎        "/help" => {
📎            eprintln!("/model [id]  show picker or set model\n/clear       clear context and tool history\n/exit        quit");
📎        }
📎        _ => eprintln!("unknown command {cmd} (try /help)"),
📎    }
📎    Ok(())
📎}
📎
📎fn render(ev: AgentEvent) {
📎    match ev {
📎        AgentEvent::AssistantText(t) => println!("{t}"),
📎        AgentEvent::ToolCall { name, input, .. } => {
📎            eprintln!("\u{1f527} {name} {}", compact(&input));
📎        }
📎        AgentEvent::ToolResult { output, is_error, .. } => {
📎            let mark = if is_error { "✖" } else { "✓" };
📎            let preview: String = output.lines().take(8).collect::<Vec<_>>().join("\n   ");
📎            eprintln!("   {mark} {preview}");
📎        }
📎        AgentEvent::TurnComplete { input_tokens, output_tokens } => {
📎            tracing::debug!(input_tokens, output_tokens, "turn");
📎        }
📎        AgentEvent::Done { .. } => {}
📎    }
📎}
📎
📎fn compact(v: &serde_json::Value) -> String {
📎    let s = v.to_string();
📎    if s.len() > 120 { format!("{}…", &s[..s.floor_char_boundary(120)]) } else { s }
📎} of 186 lines
[2] edit_file: Replaced 1 occurrence in /Users/toddbruss/Documents/GitHub/AgentiLoopCLI/crates/agentiloop-cli/src/main.rs [verified: true]

📎mod permission;
📎
📎use std::io::{self, BufRead, Write};
📎use std::sync::Arc;
📎
📎use agentiloop_core::{Agent, AgentConfig, AgentEvent, ToolContext};
📎use agentiloop_provider::{AnthropicProvider, ModelInfo};
📎use anyhow::Result;
📎use clap::Parser;
📎
📎/// AgentiLoop — a cross-platform agentic coding loop for your terminal.
📎#[derive(Parser, Debug)]
📎#[command(name = "agentiloop", version, about)]
📎struct Cli {
📎    /// Model id to use.
📎    #[arg(short, long, env = "AGENTILOOP_MODEL", default_value = "claude-sonnet-
❌4-
📎5")]
📎    model: String,
📎
📎    /// Skip all permission prompts (dangerous; intended for CI).
📎    #[arg(long, env = "AGENTILOOP_YES")]
📎    yes: bool,
📎
📎    /// Max provider round-trips per prompt.
📎    #[arg(long, default_value_t = 50)]
📎    max_turns: usize,
📎
📎    /// Working directory the agent operates in (defaults to cwd).
📎    #[arg(short = 'C', long)]
📎    cwd: Option<std::path::PathBuf>,
📎
📎    /// One-shot prompt. If omitted, starts an interactive REPL.
📎    prompt: Vec<String>,
📎}
📎
📎#[tokio::main]
📎async fn main() -> Result<()> {
📎    tracing_subscriber::fmt()
📎        .with_env_filter(tracing_subscriber::EnvFilter::from_default_env())
📎        .with_writer(io::stderr)
📎        .init();
📎
📎    let cli = Cli::parse();
📎
📎    let cwd = match cli.cwd {
📎        Some(p) => p.canonicalize()?,
📎        None => std::env::current_dir()?,
📎    };
📎
📎    let provider = Arc::new(AnthropicProvider::from_env()?);
📎    let tools = agentiloop_tools::default_registry();
📎    let policy = permission::policy(cli.yes);
📎    let config = AgentConfig { model: cli.model, max_turns: cli.max_turns, ..Default::default() };
📎
📎    let mut agent = Agent::new(provider, tools, policy, config, ToolContext { cwd: cwd.clone() });
📎
📎    if !cli.prompt.is_empty() {
📎        return agent.run(&cli.prompt.join(" "), render).await;
📎    }
📎
📎    eprintln!("AgentiLoop — cwd: {}  model: {}  (/help for commands)", cwd.display(), agent.model());
📎    let stdin = io::stdin();
📎    loop {
📎        eprint!("\n> ");
📎        io::stderr().flush()?;
📎        let mut line = String::new();
📎        if stdin.lock().read_line(&mut line)? == 0 {
📎            break;
📎        }
📎        let line = line.trim();
📎        if line.is_empty() {
📎            continue;
📎        }
📎        if line == "/exit" || line == "/quit" {
📎            break;
📎        }
📎        if line.starts_with('/') {
📎            slash_command(line, &mut agent)?;
📎            continue;
📎        }
📎        if let Err(e) = agent.run(line, render).await {
📎            eprintln!("error: {e:#}");
📎        }
📎    }
📎    Ok(())
📎}
📎
📎/// Fallback catalog used when `/v1/models` can't be reached (newest first).
📎const FALLBACK_MODELS: &[(&str, &str)] = &[
📎    ("claude-fable-5-1", "Claude Fable 5.1"),
📎    ("claude-opus-5", "Claude Opus 5"),
📎    ("claude-sonnet-5", "Claude Sonnet 5"),
📎    ("claude-fable-5", "Claude Fable 5"),
📎    ("claude-opus-4-8", "Claude Opus 4.8"),
📎    ("claude-opus-4-7", "Claude Opus 4.7"),
📎    ("claude-sonnet-4-6", "Claude Sonnet 4.6"),
📎    ("claude-opus-4-6", "Claude Opus 4.6"),
📎    ("claude-opus-4-5-20251101", "Claude Opus 4.5"),
📎    ("claude-haiku-4-5-20251001", "Claude Haiku 4.5"),
📎    ("claude-sonnet-4-5-20250929", "Claude Sonnet 4.5"),
📎];
📎
📎/// Live model list from the API, falling back to the static catalog.
📎async fn fetch_models(provider: &AnthropicProvider) -> Vec<ModelInfo> {
📎    match provider.list_models().await {
📎        Ok(list) if !list.is_empty() => list,
📎        Ok(_) => fallback_models(),
📎        Err(e) => {
📎            tracing::warn!("model list fetch failed, using fallback: {e:#}");
📎            fallback_models()
📎        }
📎    }
📎}
📎
📎fn fallback_models() -> Vec<ModelInfo> {
📎    FALLBACK_MODELS
📎        .iter()
📎        .map(|(id, name)| ModelInfo { id: id.to_string(), display_name: name.to_string(), created_at: String::new() })
📎        .collect()
📎}
📎
📎async fn slash_command(line: &str, agent: &mut Agent, provider: &AnthropicProvider) -> Result<()> {
📎    let (cmd, arg) = line.split_once(' ').map_or((line, ""), |(c, a)| (c, a.trim()));
📎    match cmd {
📎        "/clear" => {
📎            agent.clear();
📎            eprintln!("context and tool history cleared");
📎        }
📎        "/model" if !arg.is_empty() => {
📎            agent.set_model(arg);
📎            eprintln!("model: {}", agent.model());
📎        }
📎        "/model" => {
📎            let models = fetch_models(provider).await;
📎            for (i, m) in models.iter().enumerate() {
📎                let mark = if m.id == agent.model() { "*" } else { " " };
📎                let date = m.created_at.get(..10).unwrap_or("");
📎                eprintln!("{mark} {:>2}. {:<22} {:<28} {date}", i + 1, m.display_name, m.id);
📎            }
📎            eprint!("select [1-{}] or type a model id (enter to keep {}): ", models.len(), agent.model());
📎            io::stderr().flush()?;
📎            let mut pick = String::new();
📎            io::stdin().lock().read_line(&mut pick)?;
📎            let pick = pick.trim();
📎            if pick.is_empty() {
📎                return Ok(());
📎            }
📎            match pick.parse::<usize>() {
📎                Ok(n) if (1..=models.len()).contains(&n) => agent.set_model(models[n - 1].id.clone()),
📎                Ok(_) => {
📎                    eprintln!("out of range");
📎                    return Ok(());
📎                }
📎                Err(_) => agent.set_model(pick),
📎            }
📎            eprintln!("model: {}", agent.model());
📎        }
📎        "/help" => {
📎            eprintln!("/model [id]  show picker or set model\n/clear       clear context and tool history\n/exit        quit");
📎        }
📎        _ => eprintln!("unknown command {cmd} (try /help)"),
📎    }
📎    Ok(())
📎}
📎
📎fn render(ev: AgentEvent) {
📎    match ev {
📎        AgentEvent::AssistantText(t) => println!("{t}"),
📎        AgentEvent::ToolCall { name, input, .. } => {
📎            eprintln!("\u{1f527} {name} {}", compact(&input));
📎        }
📎        AgentEvent::ToolResult { output, is_error, .. } => {
📎            let mark = if is_error { "✖" } else { "✓" };
📎            let preview: String = output.lines().take(8).collect::<Vec<_>>().join("\n   ");
📎            eprintln!("   {mark} {preview}");
📎        }
📎        AgentEvent::TurnComplete { input_tokens, output_tokens } => {
📎            tracing::debug!(input_tokens, output_tokens, "turn");
📎        }
📎        AgentEvent::Done { .. } => {}
📎    }
📎}
📎
📎fn compact(v: &serde_json::Value) -> String {
📎    let s = v.to_string();
📎    if s.len() > 120 { format!("{}…", &s[..s.floor_char_boundary(120)]) } else { s }
📎} of 186 lines
[3] edit_file: Replaced 1 occurrence in /Users/toddbruss/Documents/GitHub/AgentiLoopCLI/crates/agentiloop-cli/src/main.rs [verified: true]

📎mod permission;
📎
📎use std::io::{self, BufRead, Write};
📎use std::sync::Arc;
📎
📎use agentiloop_core::{Agent, AgentConfig, AgentEvent, ToolContext};
📎use agentiloop_provider::{AnthropicProvider, ModelInfo};
📎use anyhow::Result;
📎use clap::Parser;
📎
📎/// AgentiLoop — a cross-platform agentic coding loop for your terminal.
📎#[derive(Parser, Debug)]
📎#[command(name = "agentiloop", version, about)]
📎struct Cli {
📎    /// Model id to use.
📎    #[arg(short, long, env = "AGENTILOOP_MODEL", default_value = "claude-sonnet-5")]
📎    model: String,
📎
📎    /// Skip all permission prompts (dangerous; intended for CI).
📎    #[arg(long, env = "AGENTILOOP_YES")]
📎    yes: bool,
📎
📎    /// Max provider round-trips per prompt.
📎    #[arg(long, default_value_t = 50)]
📎    max_turns: usize,
📎
📎    /// Working directory the agent operates in (defaults to cwd).
📎    #[arg(short = 'C', long)]
📎    cwd: Option<std::path::PathBuf>,
📎
📎    /// One-shot prompt. If omitted, starts an interactive REPL.
📎    prompt: Vec<String>,
📎}
📎
📎#[tokio::main]
📎async fn main() -> Result<()> {
📎    tracing_subscriber::fmt()
📎        .with_env_filter(tracing_subscriber::EnvFilter::from_default_env())
📎        .with_writer(io::stderr)
📎        .init();
📎
📎    let cli = Cli::parse();
📎
📎    let cwd = match cli.cwd {
📎        Some(p) => p.canonicalize()?,
📎        None => std::env::current_dir()?,
📎    };
📎
📎    let provider = Arc::new(AnthropicProvider::from_env()?);
📎    let tools = agentiloop_tools::default_registry();
📎    let policy = permission::policy(cli.yes);
📎    let config = AgentConfig { model: cli.model, max_turns: cli.max_turns, ..Default::default() };
📎
📎    let mut agent = Agent::new(provider
✅.clone()
📎, tools, policy, config, ToolContext { cwd: cwd.clone() });
📎
📎    if !cli.prompt.is_empty() {
📎        return agent.run(&cli.prompt.join(" "), render).await;
📎    }
📎
📎    eprintln!("AgentiLoop — cwd: {}  model: {}  (/help for commands)", cwd.display(), agent.model());
📎    let stdin = io::stdin();
📎    loop {
📎        eprint!("\n> ");
📎        io::stderr().flush()?;
📎        let mut line = String::new();
📎        if stdin.lock().read_line(&mut line)? == 0 {
📎            break;
📎        }
📎        let line = line.trim();
📎        if line.is_empty() {
📎            continue;
📎        }
📎        if line == "/exit" || line == "/quit" {
📎            break;
📎        }
📎        if line.starts_with('/') {
📎            slash_command(line, &mut agent)?;
📎            continue;
📎        }
📎        if let Err(e) = agent.run(line, render).await {
📎            eprintln!("error: {e:#}");
📎        }
📎    }
📎    Ok(())
📎}
📎
📎/// Fallback catalog used when `/v1/models` can't be reached (newest first).
📎const FALLBACK_MODELS: &[(&str, &str)] = &[
📎    ("claude-fable-5-1", "Claude Fable 5.1"),
📎    ("claude-opus-5", "Claude Opus 5"),
📎    ("claude-sonnet-5", "Claude Sonnet 5"),
📎    ("claude-fable-5", "Claude Fable 5"),
📎    ("claude-opus-4-8", "Claude Opus 4.8"),
📎    ("claude-opus-4-7", "Claude Opus 4.7"),
📎    ("claude-sonnet-4-6", "Claude Sonnet 4.6"),
📎    ("claude-opus-4-6", "Claude Opus 4.6"),
📎    ("claude-opus-4-5-20251101", "Claude Opus 4.5"),
📎    ("claude-haiku-4-5-20251001", "Claude Haiku 4.5"),
📎    ("claude-sonnet-4-5-20250929", "Claude Sonnet 4.5"),
📎];
📎
📎/// Live model list from the API, falling back to the static catalog.
📎async fn fetch_models(provider: &AnthropicProvider) -> Vec<ModelInfo> {
📎    match provider.list_models().await {
📎        Ok(list) if !list.is_empty() => list,
📎        Ok(_) => fallback_models(),
📎        Err(e) => {
📎            tracing::warn!("model list fetch failed, using fallback: {e:#}");
📎            fallback_models()
📎        }
📎    }
📎}
📎
📎fn fallback_models() -> Vec<ModelInfo> {
📎    FALLBACK_MODELS
📎        .iter()
📎        .map(|(id, name)| ModelInfo { id: id.to_string(), display_name: name.to_string(), created_at: String::new() })
📎        .collect()
📎}
📎
📎async fn slash_command(line: &str, agent: &mut Agent, provider: &AnthropicProvider) -> Result<()> {
📎    let (cmd, arg) = line.split_once(' ').map_or((line, ""), |(c, a)| (c, a.trim()));
📎    match cmd {
📎        "/clear" => {
📎            agent.clear();
📎            eprintln!("context and tool history cleared");
📎        }
📎        "/model" if !arg.is_empty() => {
📎            agent.set_model(arg);
📎            eprintln!("model: {}", agent.model());
📎        }
📎        "/model" => {
📎            let models = fetch_models(provider).await;
📎            for (i, m) in models.iter().enumerate() {
📎                let mark = if m.id == agent.model() { "*" } else { " " };
📎                let date = m.created_at.get(..10).unwrap_or("");
📎                eprintln!("{mark} {:>2}. {:<22} {:<28} {date}", i + 1, m.display_name, m.id);
📎            }
📎            eprint!("select [1-{}] or type a model id (enter to keep {}): ", models.len(), agent.model());
📎            io::stderr().flush()?;
📎            let mut pick = String::new();
📎            io::stdin().lock().read_line(&mut pick)?;
📎            let pick = pick.trim();
📎            if pick.is_empty() {
📎                return Ok(());
📎            }
📎            match pick.parse::<usize>() {
📎                Ok(n) if (1..=models.len()).contains(&n) => agent.set_model(models[n - 1].id.clone()),
📎                Ok(_) => {
📎                    eprintln!("out of range");
📎                    return Ok(());
📎                }
📎                Err(_) => agent.set_model(pick),
📎            }
📎            eprintln!("model: {}", agent.model());
📎        }
📎        "/help" => {
📎            eprintln!("/model [id]  show picker or set model\n/clear       clear context and tool history\n/exit        quit");
📎        }
📎        _ => eprintln!("unknown command {cmd} (try /help)"),
📎    }
📎    Ok(())
📎}
📎
📎fn render(ev: AgentEvent) {
📎    match ev {
📎        AgentEvent::AssistantText(t) => println!("{t}"),
📎        AgentEvent::ToolCall { name, input, .. } => {
📎            eprintln!("\u{1f527} {name} {}", compact(&input));
📎        }
📎        AgentEvent::ToolResult { output, is_error, .. } => {
📎            let mark = if is_error { "✖" } else { "✓" };
📎            let preview: String = output.lines().take(8).collect::<Vec<_>>().join("\n   ");
📎            eprintln!("   {mark} {preview}");
📎        }
📎        AgentEvent::TurnComplete { input_tokens, output_tokens } => {
📎            tracing::debug!(input_tokens, output_tokens, "turn");
📎        }
📎        AgentEvent::Done { .. } => {}
📎    }
📎}
📎
📎fn compact(v: &serde_json::Value) -> String {
📎    let s = v.to_string();
📎    if s.len() > 120 { format!("{}…", &s[..s.floor_char_boundary(120)]) } else { s }
📎} of 186 lines
[4] edit_file: Replaced 1 occurrence in /Users/toddbruss/Documents/GitHub/AgentiLoopCLI/crates/agentiloop-cli/src/main.rs [verified: true]

📎mod permission;
📎
📎use std::io::{self, BufRead, Write};
📎use std::sync::Arc;
📎
📎use agentiloop_core::{Agent, AgentConfig, AgentEvent, ToolContext};
📎use agentiloop_provider::{AnthropicProvider, ModelInfo};
📎use anyhow::Result;
📎use clap::Parser;
📎
📎/// AgentiLoop — a cross-platform agentic coding loop for your terminal.
📎#[derive(Parser, Debug)]
📎#[command(name = "agentiloop", version, about)]
📎struct Cli {
📎    /// Model id to use.
📎    #[arg(short, long, env = "AGENTILOOP_MODEL", default_value = "claude-sonnet-5")]
📎    model: String,
📎
📎    /// Skip all permission prompts (dangerous; intended for CI).
📎    #[arg(long, env = "AGENTILOOP_YES")]
📎    yes: bool,
📎
📎    /// Max provider round-trips per prompt.
📎    #[arg(long, default_value_t = 50)]
📎    max_turns: usize,
📎
📎    /// Working directory the agent operates in (defaults to cwd).
📎    #[arg(short = 'C', long)]
📎    cwd: Option<std::path::PathBuf>,
📎
📎    /// One-shot prompt. If omitted, starts an interactive REPL.
📎    prompt: Vec<String>,
📎}
📎
📎#[tokio::main]
📎async fn main() -> Result<()> {
📎    tracing_subscriber::fmt()
📎        .with_env_filter(tracing_subscriber::EnvFilter::from_default_env())
📎        .with_writer(io::stderr)
📎        .init();
📎
📎    let cli = Cli::parse();
📎
📎    let cwd = match cli.cwd {
📎        Some(p) => p.canonicalize()?,
📎        None => std::env::current_dir()?,
📎    };
📎
📎    let provider = Arc::new(AnthropicProvider::from_env()?);
📎    let tools = agentiloop_tools::default_registry();
📎    let policy = permission::policy(cli.yes);
📎    let config = AgentConfig { model: cli.model, max_turns: cli.max_turns, ..Default::default() };
📎
📎    let mut agent = Agent::new(provider.clone(), tools, policy, config, ToolContext { cwd: cwd.clone() });
📎
📎    if !cli.prompt.is_empty() {
📎        return agent.run(&cli.prompt.join(" "), render).await;
📎    }
📎
📎    eprintln!("AgentiLoop — cwd: {}  model: {}  (/help for commands)", cwd.display(), agent.model());
📎    let stdin = io::stdin();
📎    loop {
📎        eprint!("\n> ");
📎        io::stderr().flush()?;
📎        let mut line = String::new();
📎        if stdin.lock().read_line(&mut line)? == 0 {
📎            break;
📎        }
📎        let line = line.trim();
📎        if line.is_empty() {
📎            continue;
📎        }
📎        if line == "/exit" || line == "/quit" {
📎            break;
📎        }
📎        if line.starts_with('/') {
📎            slash_command(line, &mut agent
❌)
✅, &provider).await
📎?;
📎            continue;
📎        }
📎        if let Err(e) = agent.run(line, render).await {
📎            eprintln!("error: {e:#}");
📎        }
📎    }
📎    Ok(())
📎}
📎
📎/// Fallback catalog used when `/v1/models` can't be reached (newest first).
📎const FALLBACK_MODELS: &[(&str, &str)] = &[
📎    ("claude-fable-5-1", "Claude Fable 5.1"),
📎    ("claude-opus-5", "Claude Opus 5"),
📎    ("claude-sonnet-5", "Claude Sonnet 5"),
📎    ("claude-fable-5", "Claude Fable 5"),
📎    ("claude-opus-4-8", "Claude Opus 4.8"),
📎    ("claude-opus-4-7", "Claude Opus 4.7"),
📎    ("claude-sonnet-4-6", "Claude Sonnet 4.6"),
📎    ("claude-opus-4-6", "Claude Opus 4.6"),
📎    ("claude-opus-4-5-20251101", "Claude Opus 4.5"),
📎    ("claude-haiku-4-5-20251001", "Claude Haiku 4.5"),
📎    ("claude-sonnet-4-5-20250929", "Claude Sonnet 4.5"),
📎];
📎
📎/// Live model list from the API, falling back to the static catalog.
📎async fn fetch_models(provider: &AnthropicProvider) -> Vec<ModelInfo> {
📎    match provider.list_models().await {
📎        Ok(list) if !list.is_empty() => list,
📎        Ok(_) => fallback_models(),
📎        Err(e) => {
📎            tracing::warn!("model list fetch failed, using fallback: {e:#}");
📎            fallback_models()
📎        }
📎    }
📎}
📎
📎fn fallback_models() -> Vec<ModelInfo> {
📎    FALLBACK_MODELS
📎        .iter()
📎        .map(|(id, name)| ModelInfo { id: id.to_string(), display_name: name.to_string(), created_at: String::new() })
📎        .collect()
📎}
📎
📎async fn slash_command(line: &str, agent: &mut Agent, provider: &AnthropicProvider) -> Result<()> {
📎    let (cmd, arg) = line.split_once(' ').map_or((line, ""), |(c, a)| (c, a.trim()));
📎    match cmd {
📎        "/clear" => {
📎            agent.clear();
📎            eprintln!("context and tool history cleared");
📎        }
📎        "/model" if !arg.is_empty() => {
📎            agent.set_model(arg);
📎            eprintln!("model: {}", agent.model());
📎        }
📎        "/model" => {
📎            let models = fetch_models(provider).await;
📎            for (i, m) in models.iter().enumerate() {
📎                let mark = if m.id == agent.model() { "*" } else { " " };
📎                let date = m.created_at.get(..10).unwrap_or("");
📎                eprintln!("{mark} {:>2}. {:<22} {:<28} {date}", i + 1, m.display_name, m.id);
📎            }
📎            eprint!("select [1-{}] or type a model id (enter to keep {}): ", models.len(), agent.model());
📎            io::stderr().flush()?;
📎            let mut pick = String::new();
📎            io::stdin().lock().read_line(&mut pick)?;
📎            let pick = pick.trim();
📎            if pick.is_empty() {
📎                return Ok(());
📎            }
📎            match pick.parse::<usize>() {
📎                Ok(n) if (1..=models.len()).contains(&n) => agent.set_model(models[n - 1].id.clone()),
📎                Ok(_) => {
📎                    eprintln!("out of range");
📎                    return Ok(());
📎                }
📎                Err(_) => agent.set_model(pick),
📎            }
📎            eprintln!("model: {}", agent.model());
📎        }
📎        "/help" => {
📎            eprintln!("/model [id]  show picker or set model\n/clear       clear context and tool history\n/exit        quit");
📎        }
📎        _ => eprintln!("unknown command {cmd} (try /help)"),
📎    }
📎    Ok(())
📎}
📎
📎fn render(ev: AgentEvent) {
📎    match ev {
📎        AgentEvent::AssistantText(t) => println!("{t}"),
📎        AgentEvent::ToolCall { name, input, .. } => {
📎            eprintln!("\u{1f527} {name} {}", compact(&input));
📎        }
📎        AgentEvent::ToolResult { output, is_error, .. } => {
📎            let mark = if is_error { "✖" } else { "✓" };
📎            let preview: String = output.lines().take(8).collect::<Vec<_>>().join("\n   ");
📎            eprintln!("   {mark} {preview}");
📎        }
📎        AgentEvent::TurnComplete { input_tokens, output_tokens } => {
📎            tracing::debug!(input_tokens, output_tokens, "turn");
📎        }
📎        AgentEvent::Done { .. } => {}
📎    }
📎}
📎
📎fn compact(v: &serde_json::Value) -> String {
📎    let s = v.to_string();
📎    if s.len() > 120 { format!("{}…", &s[..s.floor_char_boundary(120)]) } else { s }
📎} of 186 lines
[5] edit_file: Error: edit_file refused — this file had not been read this task. It has now been read FOR YOU and is marked as read. Do NOT call file(action:"read") on it. Your very next call must be the same edit_file, with old_string/source/line numbers copied verbatim from the content below.

/Users/toddbruss/Documents/GitHub/AgentiLoopCLI/crates/agentiloop-core/src/agent.rs:
1  	use std::sync::Arc;
2  	
3  	use crate::message::{ContentBlock, Message, StopReason};
4  	use crate::permission::{Permission, SharedPolicy};
5  	use crate::provider::{Provider, ProviderRequest, ToolSpec};
6  	use crate::tool::{ToolContext, ToolError, ToolRegistry};
7  	
8  	#[derive(Debug, Clone)]
9  	pub struct AgentConfig {
10 	    pub model: String,
11 	    pub system_prompt: String,
12 	    pub max_tokens: u32,
13 	    /// Hard cap on provider round-trips per `run` to avoid runaway loops.
14 	    pub max_turns: usize,
15 	}
16 	
17 	impl Default for AgentConfig {
18 	    fn default() -> Self {
19 	        Self {
20 	            model: "claude-sonnet-4-5".into(),
21 	            system_prompt: DEFAULT_SYSTEM_PROMPT.into(),
22 	            max_tokens: 8192,
23 	            max_turns: 50,
24 	        }
25 	    }
26 	}
27 	
28 	pub const DEFAULT_SYSTEM_PROMPT: &str = "You are AgentiLoop, an autonomous coding agent running in the user's terminal. \
29 	Use the provided tools to inspect and modify the project in the current working directory. \
30 	Be concise. Prefer acting over asking. When the task is complete, reply with a short summary.";
31 	
32 	/// Events emitted during a run so the front-end can render progress.
33 	#[derive(Debug, Clone)]
34 	pub enum AgentEvent {
35 	    AssistantText(String),
36 	    ToolCall { id: String, name: String, input: serde_json::Value },
37 	    ToolResult { id: String, name: String, output: String, is_error: bool },
38 	    TurnComplete { input_tokens: u64, output_tokens: u64 },
39 	    Done { stop_reason: StopReason },
40 	}
41 	
42 	pub struct Agent {
43 	    provider: Arc<dyn Provider>,
44 	    tools: ToolRegistry,
45 	    policy: SharedPolicy,
46 	    config: AgentConfig,
47 	    ctx: ToolContext,
48 	    pub history: Vec<Message>,
49 	}
50 	
51 	impl Agent {
52 	    pub fn new(
53 	        provider: Arc<dyn Provider>,
54 	        tools: ToolRegistry,
55 	        policy: SharedPolicy,
56 	        config: AgentConfig,
57 	        ctx: ToolContext,
58 	    ) -> Self {
59 	        Self { provider, tools, policy, config, ctx, history: Vec::new() }
60 	    }
61 	
62 	    pub fn model(&self) -> &str {
63 	        &self.config.model
64 	    }
65 	
66 	    pub fn set_model(&mut self, model: impl Into<String>) {
67 	        self.config.model = model.into();
68 	    }
69 	
70 	    /// Drop all conversation context and tool history.
71 	    pub fn clear(&mut self) {
72 	        self.history.clear();
73 	    }
74 	
75 	    fn tool_specs(&self) -> Vec<ToolSpec> {
76 	        self.tools
77 	            .iter()
78 	            .map(|t| ToolSpec {
79 	                name: t.name().to_string(),
80 	                description: t.description().to_string(),
81 	                input_schema: t.input_schema(),
82 	            })
83 	            .collect()
84 	    }
85 	
86 	    /// The core agentic loop: send → if tool_use, execute tools, append results, repeat.
87 	    pub async fn run<F>(&mut self, user_input: &str, mut on_event: F) -> anyhow::Result<()>
88 	    where
89 	        F: FnMut(AgentEvent),
90 	    {
91 	        self.history.push(Message::user_text(user_input));
92 	
93 	        for _ in 0..self.config.max_turns {
94 	            let req = ProviderRequest {
95 	                model: self.config.model.clone(),
96 	                system: self.config.system_prompt.clone(),
97 	                messages: self.history.clone(),
98 	                tools: self.tool_specs(),
99 	                max_tokens: self.config.max_tokens,
100	            };
101	
102	            let resp = self.provider.complete(req).await?;
103	            on_event(AgentEvent::TurnComplete {
104	                input_tokens: resp.input_tokens,
105	                output_tokens: resp.output_tokens,
106	            });
107	
108	            let text = resp.message.text();
109	            if !text.is_empty() {
110	                on_event(AgentEvent::AssistantText(text));
111	            }
112	
113	            let calls: Vec<(String, String, serde_json::Value)> = resp
114	                .message
115	                .tool_uses()
116	                .map(|(id, name, input)| (id.to_string(), name.to_string(), input.clone()))
117	                .collect();
118	
119	            self.history.push(resp.message.clone());
120	
121	            if resp.stop_reason != StopReason::ToolUse || calls.is_empty() {
122	                on_event(AgentEvent::Done { stop_reason: resp.stop_reason });
123	                return Ok(());
124	            }
125	
126	            let mut results = Vec::with_capacity(calls.len());
127	            for (id, name, input) in calls {
128	                on_event(AgentEvent::ToolCall { id: id.clone(), name: name.clone(), input: input.clone() });
129	                let (output, is_error) = match self.execute(&name, input).await {
130	                    Ok(out) => (out, false),
131	                    Err(e) => (e.to_string(), true),
132	                };
133	                on_event(AgentEvent::ToolResult {
134	                    id: id.clone(),
135	                    name,
136	                    output: output.clone(),
137	                    is_error,
138	                });
139	                results.push(ContentBlock::ToolResult { tool_use_id: id, content: output, is_error });
140	            }
141	            self.history.push(Message::tool_results(results));
142	        }
143	
144	        anyhow::bail!("max_turns ({}) reached", self.config.max_turns)
145	    }
146	
147	    async fn execute(&self, name: &str, input: serde_json::Value) -> Result<String, ToolError> {
148	        let tool = self
149	            .tools
150	            .get(name)
151	            .ok_or_else(|| ToolError::InvalidInput(format!("unknown tool `{name}`")))?;
152	
153	        if self.policy.check(name, tool.is_mutating(), &input).await == Permission::Deny {
154	            return Err(ToolError::Denied(format!("user declined `{name}`")));
155	        }
156	
157	        tool.call(&self.ctx, input).await
158	    }
159	}
160	

● 5/5 tasks completed